exports.spotify_critera_match = function (job, done) {
 
	/*
		query nokia db for isrc - track name
		query spotiofy crawl table - track name
		compare results..
		if (pass_validation) {
			Dispatch.createGraiInsertJob(...)
		}
	*/

}